import React from 'react';
import { Link }from 'react-router-dom';

const Logo = () => {
    return (
        <Link to="/" className="brand-logo">LogoThisIs</Link>
        
    );
}

export default Logo;
